services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fisure
      POSTGRES_PASSWORD: fisure
      POSTGRES_DB: fisure
    ports:
      - "5432:5432"
    volumes:
      - fisure_pg:/var/lib/postgresql/data

volumes:
  fisure_pg:
